#include <stdio.h>
/*
 * Main - is beginnig of function
 *
 * Function: Prints numbers to nine
 *
 * Return: is always 0
 */
int main(void)
{
	int x;
	for (x=0; x<10; x++)
		printf("%d",x);
	return (0);
}
